stages:
  - playwright-test

test:
  image: mcr.microsoft.com/playwright:v1.43.1-jammy
  stage: playwright-test
  script:
    - npm install
    - npm run test -- --workers=4
    - ls -R
  when: manual
  artifacts:
    when: always
    paths:
      - playwright-report
    expire_in: 1 day